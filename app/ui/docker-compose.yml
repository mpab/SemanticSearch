version: "3.4"

services:
  semantic_search_ui:
    image: semantic_search_ui
    container_name: semantic_search_ui
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
    volumes:
      - ./:/app/ui
